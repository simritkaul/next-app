"use client";
import React from "react";

const AddToCart = () => {
  return (
    <>
      <div>
        <button onClick={() => console.log("Added")}>Add</button>
      </div>
    </>
  );
};

export default AddToCart;
